(base) [jellepoland@fedora SymbolicAWEModels.jl]$ git diff HEAD..af7cb8c
diff --git a/data/base/settings.yaml b/data/base/settings.yaml
index 9a99d84..98db7e1 100644
--- a/data/base/settings.yaml
+++ b/data/base/settings.yaml
@@ -47,7 +47,6 @@ tether:
     rho_tether:  724.0     # density of Dyneema           [kg/m³]
     e_tether: 55000000000.0 # axial tensile modulus of Dyneema (M.B. Ruppert) [Pa]
                            # SK75: 109 to 132 GPa according to datasheet
-
 winch:
     winch_model: "TorqueControlledMachine" # or AsynchMachine
     max_force: 4000        # maximal (nominal) tether force; short overload allowed [N]
diff --git a/data/tutorial_1/settings.yaml b/data/tutorial_1/settings.yaml
deleted file mode 100644
index 9a74f6d..0000000
--- a/data/tutorial_1/settings.yaml
+++ /dev/null
@@ -1,83 +0,0 @@
-system:
-    log_file: "data/ram_air_kite"  # filename without extension  [replay only]
-                                   #   use / as path delimiter, even on Windows 
-    time_lapse:   1.0              # relative replay speed
-    sim_time:   100.0              # simulation time             [sim only]
-    segments:       3              # number of tether segments
-    sample_freq:   20              # sample frequency in Hz
-    zoom:        0.03              # zoom factor for the system view
-    kite_scale:   3.0              # relative zoom factor for the 4 point kite
-    fixed_font: ""                 # name or filepath+filename of alternative fixed pitch font
-    g_earth:     9.81
-
-initial:
-    l_tethers: [50.0, 50.0, 50.0]  # initial tether length       [m]
-    elevation: 70.8                # initial elevation angle   [deg]
-    v_reel_outs: [0.0, 0.0, 0.0]   # initial reel out speed    [m/s]
-
-solver:
-    solver: "FBDF"
-    abs_tol: 0.01          # absolute tolerance of the DAE solver [m, m/s]
-    rel_tol: 0.01          # relative tolerance of the DAE solver [-]
-    relaxation: 0.6        # relaxation factor of inner linear Newton solver, needed for quasi-steady solver
-
-kite:
-    model: "data/ram_air_kite_body.obj"     # 3D model of the kite
-    foil_file: "data/ram_air_kite_foil.dat" # filename for the foil shape
-    physical_model: "ram"            # name of the kite model to use (KPS3, KPS4 or SymbolicAWEModel)
-    top_bridle_points:                      # top bridle points that are not on the kite body in CAD frame
-        - [0.290199, 0.784697, -2.61305]
-        - [0.392683, 0.785271, -2.61201]
-        - [0.498202, 0.786175, -2.62148]
-        - [0.535543, 0.786175, -2.62148]
-    crease_frac: 0.82                       # distance along normalized foil chord for the trailing edge deformation crease
-    bridle_fracs: [0.088, 0.31, 0.58, 0.93] # distances along normalized foil chord for bridle attachment points
-    mass: 0.9                               # kite mass [kg]
-    quasi_static: false                     # whether to use quasi static kite points or not
-
-tether:
-    bridle_tether_diameter: 2 # [mm]
-    power_tether_diameter: 2 # [mm]
-    steering_tether_diameter: 1 # [mm]
-    cd_tether: 0.958       # drag coefficient of the tether
-    damping: 473.0         # unit damping coefficient        [Ns]
-    c_spring: 614600.0     # unit spring constant coefficient [N]
-    rho_tether:  724.0     # density of Dyneema           [kg/m³]
-    e_tether: 55000000000.0 # axial tensile modulus of Dyneema (M.B. Ruppert) [Pa]
-                           # SK75: 109 to 132 GPa according to datasheet
-
-winch:
-    winch_model: "TorqueControlledMachine" # or AsynchMachine
-    max_force: 4000        # maximal (nominal) tether force; short overload allowed [N]
-    v_ro_max:  8.0         # maximal reel-out speed                          [m/s]
-    v_ro_min: -8.0         # minimal reel-out speed (=max reel-in speed)     [m/s]
-    drum_radius: 0.110    # radius of the drum                              [m]
-    max_acc: 4.0           # maximal acceleration of the winch               [m/s²]
-    gear_ratio: 1.0        # gear ratio of the winch                         [-]   
-    inertia_total: 0.024   # total inertia, as seen from the motor/generator [kgm²]
-    f_coulomb: 122.0       # coulomb friction                                [N]
-    c_vf: 30.6             # coefficient for the viscous friction            [Ns/m]
-    p_speed: 1.0           # proportional gain of the winch speed controller [-]
-    i_speed: 0.1           # integral gain of the winch speed controller     [-]
-
-environment:
-    v_wind: 15.51            # wind speed at reference height          [m/s]
-    upwind_dir: -90.0        # upwind direction                        [deg]
-    temp_ref: 15.0           # temperature at reference height         [°C]
-    height_gnd: 0.0          # height of groundstation above see level [m]
-    h_ref:  6.0              # reference height for the wind speed     [m]
-
-    rho_0:  1.225            # air density at zero height and 15 °C    [kg/m³]
-    alpha:  0.08163          # exponent of the wind profile law
-    z0:     0.0002           # surface roughness                       [m]
-    profile_law: 3           # 1=EXP, 2=LOG, 3=EXPLOG, 4=FAST_EXP, 5=FAST_LOG, 6=FAST_EXPLOG
-    # the following parameters are for calculating the turbulent wind field using the Mann model
-    use_turbulence: 0.0      # turbulence intensity relative to Cabau, NL
-    v_wind_gnds: [3.483, 5.324, 8.163] # wind speeds at ref height for calculating the turbulent wind field [m/s]
-    avg_height: 200.0        # average height during reel out          [m]
-    rel_turbs:   [0.342, 0.465, 0.583] # relative turbulence at the v_wind_gnds
-    i_ref: 0.14              # is the expected value of the turbulence intensity at 15 m/s.
-    v_ref: 42.9              # five times the average wind speed in m/s at hub height over the full year    [m/s]
-                             # Cabau: 8.5863 m/s * 5.0 = 42.9 m/s
-    height_step: 2.0         # use a grid with 2m resolution in z direction                                 [m]
-    grid_step:   2.0         # grid resolution in x and y direction                                         [m]               
diff --git a/data/tutorial_1/system.yaml b/data/tutorial_1/system.yaml
deleted file mode 100644
index 0ee4aae..0000000
--- a/data/tutorial_1/system.yaml
+++ /dev/null
@@ -1,3 +0,0 @@
-system:
-    sim_settings: "settings.yaml"  # simulator settings
- 
\ No newline at end of file
diff --git a/data/tutorial_1/vsm_settings.yaml b/data/tutorial_1/vsm_settings.yaml
deleted file mode 100644
index 5ae66dc..0000000
--- a/data/tutorial_1/vsm_settings.yaml
+++ /dev/null
@@ -1,36 +0,0 @@
-Model:
-    VSM: Vortex Step Method
-    LLT: Lifting Line Theory
-PanelDistribution:
-    LINEAR:             Linear distribution
-    COSINE:             Cosine distribution
-    COSINE_VAN_GARREL:  van Garrel cosine distribution
-    SPLIT_PROVIDED:     Split provided sections
-    UNCHANGED:          Keep original sections
-InitialGammaDistribution:
-    ELLIPTIC:           Elliptic distribution
-    ZEROS:              Constant distribution
-
-wings:
-    - name: main_wing
-      n_panels: 40
-      n_groups: 40
-      spanwise_panel_distribution: LINEAR
-      spanwise_direction: [0.0, 1.0, 0.0]
-      remove_nan: true
-solver_settings:
-    n_panels: 40
-    n_groups: 40
-    aerodynamic_model_type: VSM
-    density: 1.225                   # air density  [kg/m³]
-    max_iterations: 1500
-    rtol: 1e-5                       # relative error   [-]
-    tol_reference_error: 0.001
-    relaxation_factor: 0.03          # relaxation factor for convergence
-    artificial_damping: false        # whether to apply artificial damping
-    k2: 0.1                          # artificial damping parameter
-    k4: 0.0                          # artificial damping parameter
-    type_initial_gamma_distribution: ELLIPTIC
-    core_radius_fraction: 1e-20
-    mu: 1.81e-5                      # dynamic viscosity [N·s/m²]
-    calc_only_f_and_gamma: false     # whether to only output f and gamma
diff --git a/examples/catenary_line.jl b/examples/catenary_line.jl
index 607fd57..71ab102 100644
--- a/examples/catenary_line.jl
+++ b/examples/catenary_line.jl
@@ -10,6 +10,10 @@ This script sets up a simple system with multiple tether segments, fixed at two
 at equal height, under gravity. The simulation compares the final shape to
 the analytical catenary solution.
 
+This example also demonstrates the use of world frame bridle damping, where the
+damping force is proportional to the absolute velocity of each point rather than
+relative to a wing body.
+
 Requirements:
 - SymbolicAWEModels
 - ControlPlots
@@ -17,6 +21,8 @@ Requirements:
 
 using SymbolicAWEModels, VortexStepMethod, ControlPlots
 
+println("\n\nCatenary Line Example\n", "="^40)
+
 # --- Settings
 set = load_settings("base")  # Loads from data/base/settings.yaml
 set.v_wind = 0.0               # No wind for pure catenary
@@ -28,23 +34,24 @@ set.l_tether = 8        # Set tether length for plot size
 horizontal_span = 8          # Horizontal distance between anchors [m]
 n_segments = 10                # Number of tether segments (n points = n_segments+1)
 total_length = 10.0            # Total unstretched length of tether [m]
-segment_mass = 0.5             # Mass per segment [kg]
+l0_per_segment = total_length / n_segments  # Rest length per segment
 compression_frac = 0.01      # Compression fraction for segments
+world_frame_damping = 1e3   # World frame damping coefficient [Ns/m] - INCREASED TO TEST EFFECT
 
 # --- Points (nodes)
 points = Point[]
 push!(points, Point(1, [0.0, 0.0, 5.0], STATIC))                               # Left anchor
 
-# Add dynamic points along initial straight line
+# Add dynamic points along initial straight line with world frame bridle damping
 for i in 1:n_segments-1
     x = i * horizontal_span / n_segments
-    push!(points, Point(i+1, [x, 0.0, 5.0], DYNAMIC; mass=segment_mass))       # Points 2,3,4,...
+    push!(points, Point(i+1, [x, 0.0, 5.0], DYNAMIC; 
+                       world_frame_damping=world_frame_damping))       # Points 2,3,4,...
 end
 push!(points, Point(n_segments+1, [horizontal_span, 0.0, 5.0], STATIC))        # Right anchor
 
 # --- Segments (springs/dampers)
 segments = Segment[]
-l0_per_segment = total_length / n_segments  # Rest length per segment
 
 for i in 1:n_segments
     # Connect consecutive points
@@ -58,6 +65,22 @@ transforms = [Transform(1, 0.0, 0.0, 0.0; base_pos=[0.0, 0.0, 5.0], base_point_i
 # --- System structure
 sys_struct = SymbolicAWEModels.SystemStructure("catenary", set; points, segments, transforms)
 
+# Debug: Check masses and damping values
+println("Debug: System structure created")
+println("Number of points: ", length(sys_struct.points))
+for i in 1:length(sys_struct.points)
+    point = sys_struct.points[i]
+    println("Point $i: type=$(point.type), mass=$(point.mass), world_frame_damping=$(point.world_frame_damping), body_frame_damping=$(point.body_frame_damping)")
+end
+
+println("\nDebug: Segments")
+for i in 1:length(sys_struct.segments)
+    seg = sys_struct.segments[i]
+    println("Segment $i: points=$(seg.point_idxs), l0=$(seg.l0), diameter=$(seg.diameter)")
+end
+
+println("\nDebug: Settings")
+println("rho_tether: ", set.rho_tether)
 
 # Analyze the system, to find optimal damping
 include("damping_analysis.jl")
@@ -75,15 +98,38 @@ plot(sys_struct, 0.0; zoom=false)
 sam = SymbolicAWEModel(set, sys_struct)
 init!(sam; remake=false)
 
+#####################################################
+#####################################################
+################### --- Creating catenary line withou bridle damping
+# bridle_damping_coeff = 0.0  # No bridle damping for comparison
+# points = Point[]
+# push!(points, Point(1, [0.0, 0.0, 5.0], STATIC))        
+# for i in 1:n_segments-1
+#     x = i * horizontal_span / n_segments
+#     push!(points, Point(i+1, [x, 0.0, 5.0], DYNAMIC; 
+#                        bridle_damping=bridle_damping_coeff,
+#                        bridle_damping_frame=WORLD_FRAME))       # Points 2,3,4,...
+# end
+# push!(points, Point(n_segments+1, [horizontal_span, 0.0, 5.0], STATIC))        # Right anchor
+# segments = Segment[]
+# l0_per_segment = total_length / n_segments  # Rest length per segment
+# transforms = [Transform(1, 0.0, 0.0, 0.0; base_pos=[0.0, 0.0, 5.0], base_point_idx=1, rot_point_idx=2)]
+# sys_struct_no_damp = SymbolicAWEModels.SystemStructure("catenary_no_damp", set; points, segments, transforms)
+# sam_no_damp = SymbolicAWEModel(set, sys_struct_no_damp)
+# init!(sam_no_damp; remake=false)
+
+
 # --- Simulate until static equilibrium
 println("Simulating catenary formation...")
 n_steps = 500
 for i in 1:n_steps
     next_step!(sam)
+    # next_step!(sam_no_damp)  # Simulate without damping for comparison
     # plot(sam, i/set.sample_freq; zoom=false)
     ## Plot every 10 steps to show evolution
     if i % 5 == 0
         plot(sam, i/set.sample_freq; zoom=false)
+        # plot(sam_no_damp, i/set.sample_freq; zoom=false, title="Catenary without damping")
         # println("Step $i/$n_steps")
     end
 end
diff --git a/examples/menu.jl b/examples/menu.jl
index b22eed3..9269a6f 100644
--- a/examples/menu.jl
+++ b/examples/menu.jl
@@ -3,7 +3,6 @@
 using REPL.TerminalMenus
 
 options = [
-        "damping_analysis = include(\"damping_analysis.jl\")",
         "catenary_line = include(\"catenary_line.jl\")",
         "hanging_mass = include(\"hanging_mass.jl\")",
         "simple_pulley = include(\"simple_pulley.jl\")",
diff --git a/examples/simple_pulley.jl b/examples/simple_pulley.jl
index 0f4b3e0..bd8777c 100644
--- a/examples/simple_pulley.jl
+++ b/examples/simple_pulley.jl
@@ -4,6 +4,8 @@
 
 using SymbolicAWEModels, VortexStepMethod, ControlPlots
 
+println("\n\nSimple Pulley Example\n", "="^40)
+
 set = load_settings("base")  # Loads from data/base/settings.yaml
 set.v_wind = 0
 set.l_tether = 5.0 # set l_tether as it affects the plot size
diff --git a/src/SymbolicAWEModels.jl b/src/SymbolicAWEModels.jl
index 1ee025e..b94c068 100644
--- a/src/SymbolicAWEModels.jl
+++ b/src/SymbolicAWEModels.jl
@@ -68,6 +68,7 @@ export SymbolicAWEModel
 export SystemStructure, Point, Group, Segment, Pulley, Tether, Winch, Wing, Transform
 # Enums
 export DynamicsType, DYNAMIC, QUASI_STATIC, WING, STATIC
+export DampingFrameType, WING_FRAME, WORLD_FRAME
 export SegmentType, POWER_LINE, STEERING_LINE, BRIDLE
 
 # --- High-Level Simulation Functions (Workers) ---
diff --git a/src/generate_system.jl b/src/generate_system.jl
index 98defb1..3151ef9 100644
--- a/src/generate_system.jl
+++ b/src/generate_system.jl
@@ -177,6 +177,8 @@ get_axial_damping(sys::SystemStructure, idx::Int16) = sys.segments[idx].axial_da
 @register_symbolic get_axial_damping(sys::SystemStructure, idx::Int16)
 get_bridle_damping(sys::SystemStructure, idx::Int16) = sys.points[idx].bridle_damping
 @register_symbolic get_bridle_damping(sys::SystemStructure, idx::Int16)
+get_bridle_damping_frame(sys::SystemStructure, idx::Int16) = sys.points[idx].bridle_damping_frame
+@register_symbolic get_bridle_damping_frame(sys::SystemStructure, idx::Int16)
 get_brake(sys::SystemStructure, idx::Int16) = sys.winches[idx].brake
 @register_symbolic get_brake(sys::SystemStructure, idx::Int16)
 get_fix_point_sphere(sys::SystemStructure, idx::Int16) = sys.points[idx].fix_sphere
@@ -235,8 +237,8 @@ function force_eqs!(s, system, psys, pset, eqs, defaults, guesses;
     @unpack points, groups, segments, pulleys, tethers, winches, wings = system
     
     # ==================== POINTS ==================== #
-    tether_wing_force = zeros(Num, length(wings), 3)
-    tether_wing_moment = zeros(Num, length(wings), 3)
+    tether_wing_force = zeros(Num, max(1, length(wings)), 3)
+    tether_wing_moment = zeros(Num, max(1, length(wings)), 3)
     @variables begin
         pos(t)[1:3, eachindex(points)]
         vel(t)[1:3, eachindex(points)]
@@ -344,8 +346,12 @@ function force_eqs!(s, system, psys, pset, eqs, defaults, guesses;
             # n = sym_normalize(wing_pos)
             # n = n * (p ⋅ n)
             # r = (p - n) # https://en.wikipedia.org/wiki/Distance_from_a_point_to_a_line#Vector_formulation
-            if length(wings) > 0
-                bridle_damp_vec = get_bridle_damping(psys, point.idx) * (vel[:, point.idx] - wing_vel[point.wing_idx, :])
+            if get_bridle_damping(psys, point.idx) != 0
+                if get_bridle_damping_frame(psys, point.idx) == WING_FRAME && length(wings) > 0
+                    bridle_damp_vec = get_bridle_damping(psys, point.idx) * (vel[:, point.idx] - wing_vel[point.wing_idx, :])
+                else  # WORLD_FRAME or no wings available
+                    bridle_damp_vec = get_bridle_damping(psys, point.idx) * vel[:, point.idx]
+                end
             else
                 bridle_damp_vec = zeros(3)
             end
@@ -1053,23 +1059,23 @@ function create_sys!(s::SymbolicAWEModel, system::SystemStructure; prn=true)
     @variables begin
         # potential differential variables
         set_values(t)[eachindex(winches)] = zeros(length(winches))
-        wing_pos(t)[eachindex(wings), 1:3] # xyz pos of wing in world frame
-        wing_vel(t)[eachindex(wings), 1:3]
-        wing_acc(t)[eachindex(wings), 1:3]
-        ω_b(t)[eachindex(wings), 1:3] # turn rate in principal frame
-        α_b(t)[eachindex(wings), 1:3]
+        wing_pos(t)[max(1, length(wings)), 1:3] # xyz pos of wing in world frame (at least 1x3 for empty wings)
+        wing_vel(t)[max(1, length(wings)), 1:3] # At least 1x3 to avoid index errors when no wings
+        wing_acc(t)[max(1, length(wings)), 1:3]
+        ω_b(t)[max(1, length(wings)), 1:3] # turn rate in principal frame
+        α_b(t)[max(1, length(wings)), 1:3]
 
         # rotations and frames
-        R_b_w(t)[eachindex(wings), 1:3, 1:3] # rotation of the wing body frame relative to the world frame
+        R_b_w(t)[max(1, length(wings)), 1:3, 1:3] # rotation of the wing body frame relative to the world frame (at least 1x3x3)
 
         # rest: forces, moments, vectors and scalar values
-        aero_force_b(t)[eachindex(wings), 1:3]
-        aero_moment_b(t)[eachindex(wings), 1:3]
-        twist_angle(t)[eachindex(groups)]
-        twist_ω(t)[eachindex(groups)]
-        group_aero_moment(t)[eachindex(groups)]
+        aero_force_b(t)[max(1, length(wings)), 1:3]
+        aero_moment_b(t)[max(1, length(wings)), 1:3]
+        twist_angle(t)[max(1, length(groups))]
+        twist_ω(t)[max(1, length(groups))]
+        group_aero_moment(t)[max(1, length(groups))]
         wind_vec_gnd(t)[1:3]
-        va_wing_b(t)[eachindex(wings), 1:3]
+        va_wing_b(t)[max(1, length(wings)), 1:3]
     end
 
     eqs, defaults, guesses, tether_wing_force, tether_wing_moment = 
diff --git a/src/system_structure.jl b/src/system_structure.jl
index d329023..165be5e 100644
--- a/src/system_structure.jl
+++ b/src/system_structure.jl
@@ -79,8 +79,7 @@ function load_settings(subdirectory::String)
         write(system_yaml_path, yaml_content)
         
         # Load settings using KiteUtils
-        set = Settings("system.yaml")
-        
+        set = Settings("system.yaml")        
         return set
         
     finally
@@ -131,7 +130,6 @@ Enumeration for the dynamic model governing a point's motion.
     WING
     STATIC
 end
-
 """
     mutable struct Point
 
@@ -151,7 +149,8 @@ mutable struct Point
     const force::KVec3
     const type::DynamicsType
     mass::SimFloat
-    bridle_damping::SimFloat
+    body_frame_damping::SimFloat
+    world_frame_damping::SimFloat
     fix_sphere::Bool
 end
 
@@ -184,6 +183,7 @@ where:
 - `transform_idx::Int16=1`: Index of the transform used for initial positioning.
 - `mass::Float64=0.0`: Mass of the point [kg].
 - `bridle_damping::Float64=0.0`: Damping coefficient for bridle points.
+- `bridle_damping_frame::DampingFrameType=WING_FRAME`: Reference frame for damping.
 - `fix_sphere::Bool=false`: If true, constrains the point to a sphere.
 
 # Returns
@@ -191,10 +191,10 @@ where:
 """
 function Point(idx, pos_cad, type;
     wing_idx=1, vel_w=zeros(KVec3), transform_idx=1, 
-    mass=0.0, bridle_damping=0.0, fix_sphere=false
+    mass=0.0, body_frame_damping=0.0, world_frame_damping=0.0, fix_sphere=false
 )
     Point(idx, transform_idx, wing_idx, pos_cad, zeros(KVec3), zeros(KVec3),
-        vel_w, zeros(KVec3), zeros(KVec3), type, mass, bridle_damping, fix_sphere)
+        vel_w, zeros(KVec3), zeros(KVec3), type, mass, body_frame_damping, world_frame_damping, fix_sphere)
 end
 
 """
(base) [jellepoland@fedora SymbolicAWEModels.jl]$ 